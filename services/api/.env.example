# Copy to .env and fill in for production.
PORT=4000
HOST=0.0.0.0
DATA_FILE=./data/events.json
# Shared secret the Alt1 app must send as the x-ingest-key header.
# Leave blank ONLY for local dev. Generate with: openssl rand -hex 24
INGEST_KEY=
